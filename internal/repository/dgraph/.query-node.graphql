fragment NodeFragment on Node {
	id
}

# ------------------------------------------------------------------------------

query GetNode($id: ID!) {
	getNode(id: $id) {
		__typename
		...NodeFragment
		... on Product {...ProductFragment}
		... on Component {...ComponentFragment}
		... on License {...LicenseFragment}
		... on User {...UserFragment}
		... on Group {...GroupFragment}
	}
}

query GetNodes($filter: NodeFilter, $first: Int, $offset: Int) {
	queryNode(filter: $filter, first: $first, offset: $offset) {id}
}

mutation DeleteNode($filter: NodeFilter!) {
	deleteNode(filter: $filter) {node {id}}
}
